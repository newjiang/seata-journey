server:
  port: 10002
spring:
  application:
    name: seata-order
  config:
    import:
      - optional:nacos:seata-order?group=DEFAULT_GROUP
  cloud:
    nacos:
      discovery:
        server-addr: 172.31.18.86:8848
        username: nacos
        password: 123456
        namespace: SEATA
        group: DEFAULT_GROUP
      # 配置中心配置
      config:
        server-addr: 172.31.18.86
        username: nacos
        password: 123456
        namespace: SEATA
        group: DEFAULT_GROUP
        file-extension: yaml

seata:
  data-source-proxy-mode: XA 